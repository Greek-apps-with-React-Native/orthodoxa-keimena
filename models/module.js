function Module(info) {
    this.id = info.id; 
    this.moduleCategory = info.moduleCategory;
    this.heading = info.heading;
    this.moduleText = info.moduleText;
    this.color = info.color
    this.moduleTextSmall = info.moduleTextSmall;
}
export default Module;